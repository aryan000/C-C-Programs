#include<bits/stdc++.h>
using namespace std;

int main()
{
	/* string str;
	 getline(cin,str);

     string temp; 
     cin >> temp;

	 int x = str.find(temp);
  ``
   //cout<< x << endl;

   //int p = str.find(temp,x+1);  

   str.replace(str.find(temp,x+1), temp.length()," ");
  // cout<< p << endl;
   cout<< str << endl; */

   string str;
    cin>> str;

    str.erase(str.find)

}